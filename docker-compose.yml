version: '3.8'

services:
  app:
    build: .
    restart: always
    ports:
      - "3001:3000"   # Mapeia a porta 3001 do host para a 3000 do container
    volumes:
      - wpp_session_data:/app/auth_info_final
    environment:
      - TYPEBOT_URL=${TYPEBOT_URL}

volumes:
  wpp_session_data: